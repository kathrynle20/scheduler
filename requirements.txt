nvidia-ml-py>=12.535
pyyaml>=6.0
numpy>=1.24
pandas>=2.0
pytest>=7.4
# torch is intentionally not pinned here -- install the CUDA-matched build
# on each cluster node. Workloads fall back to sleep-based stubs if torch
# or CUDA is unavailable, so simulated runs work without it.
